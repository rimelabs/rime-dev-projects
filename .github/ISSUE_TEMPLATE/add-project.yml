name: Add or update a project
description: Propose a project, integration, or developer tool for the Rime catalog.
title: "[Project]: "
labels:
  - project-submission
body:
  - type: input
    id: event
    attributes:
      label: Developer event (if applicable)
      description: Leave this blank if the project was built independently.
      placeholder: Event name and year
  - type: input
    id: project
    attributes:
      label: Project name
    validations:
      required: true
  - type: input
    id: repository
    attributes:
      label: Public source repository
      placeholder: https://github.com/owner/repository
    validations:
      required: true
  - type: textarea
    id: summary
    attributes:
      label: Brief executive summary
      description: In one or two sentences, explain what the project does, how Rime is used, and why the work is useful.
    validations:
      required: true
  - type: input
    id: article
    attributes:
      label: Article or technical write-up
      description: Optional. A repository README is acceptable.
  - type: input
    id: demo
    attributes:
      label: Demo video
      description: Optional. YouTube, Loom, or a publicly viewable recording.
  - type: textarea
    id: team
    attributes:
      label: Team members
      description: List each member’s name and LinkedIn URL. Add a GitHub URL when LinkedIn is unavailable.
    validations:
      required: true
  - type: textarea
    id: rime
    attributes:
      label: How the project uses Rime
      description: Point to the primary voice flow and the relevant source files.
    validations:
      required: true
  - type: checkboxes
    id: checks
    attributes:
      label: Publication checks
      options:
        - label: Rime is a meaningful part of the project, or this work is directly useful to developers building with Rime.
          required: true
        - label: The linked repository does not contain live credentials or private data.
          required: true
        - label: The builders consent to publishing the submitted names and profile links.
          required: true
